#include "main.h"

/**
 *print_last_digit - a function that print last digit of a number
 *@r: number's last digit value result
 * Return : value of the last number
 */
int print_last_digit(int n)
{
int r = n % 10;
if (n < 0)
{
  r = -1 * (x % 1);
}
else
{
r = x % 10;
}
_putchar(r + '0');
return (r);
}
