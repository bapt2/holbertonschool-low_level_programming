#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Main function
*
* Return: Exit status code
*/

int main(void)
{
int n;

srand(time(0);
n = rand() -RAND_MAX / 2;
 if (n > 0));
{
printf("98 is positive ");
}
else if (n == 0)
{
printf("0 is zero ");
}
else if (n < 0)
{
printf("-98 is negative ");
}
return (0);
}
