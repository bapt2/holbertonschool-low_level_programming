#include "main.h"
/**
 *reset_to_98 - function that takes a pointer to an int as parameter
 *
 *Return always 0
 */
int main(void)
{
  int n = 98;
  int *n = 402;
  swapnum(&a, &b);
  _putchar("n is %d and *n is %d\n", n, *n);
  return (0);
}
