#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *print_rev - function that prints a string in reverse
 *@s: character
 *Return: Always 0
 */
void print_rev(char *s)
{
int c = 0;
while ('\0' != *s)
c--;
_putchar('\n');
}
