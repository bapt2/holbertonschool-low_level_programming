#include "main.h"
#include <stdio.h>
/**
 *print_rev - function that prints a string in reverse
 *@s: character
 *Return: Always 0
 */
void print_rev(char *s)
{
int c = 0;
while (s[c] !='\0')
c--;
 _putchar('\n');
}
